{
  "name": "coa-codesearch-mcp-typescript",
  "version": "1.0.0",
  "description": "TypeScript support for COA CodeSearch MCP Server",
  "private": true,
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "scripts": {
    "postinstall": "echo TypeScript installed at: && node -p \"require.resolve('typescript')\""
  }
}